# SONAR.AI / AGTS Requirements
# ==============================

# Core Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# HTTP & Scraping
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
feedparser>=6.0.0
httpx>=0.25.0

# NLP — Entity extraction (optional but nice to have)
spacy>=3.7.0

# Classification — REQUIRED (at least scikit-learn)
scikit-learn>=1.3.0
numpy>=1.24.0

# Data Processing
pandas>=2.0.0

# Utilities
pydantic>=2.0.0
python-dateutil>=2.8.0

# ──────────────────────────────────────────────────────
# RECOMMENDED (install separately, not auto-installed):
#
#   pip install sentence-transformers
#
# This gives ~95% classification accuracy vs ~85% with
# TF-IDF alone. Downloads ~80MB model on first run.
# The classifier auto-detects it — no code changes.
# ──────────────────────────────────────────────────────

# Optional: LLM Integration (for future AI-generated summaries)
# openai>=1.0.0
# anthropic>=0.7.0
